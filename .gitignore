.pio/
.DS_Store
__pycache__/
*.pyc

# Flasher firmware artifacts are generated by tools/export_web_flasher.py
# (local) or fetched from GitHub Releases (Pages CI) — never committed.
web/firmware/

# Local Worker secrets (auth keys) — copy cloud/.dev.vars.example to start.
.dev.vars
